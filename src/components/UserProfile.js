import Header from "./Header";
import ManageProfile from "./ManageProfile";

const UserProfile = () => {
    return (
        <div>
            <Header/>
            <ManageProfile/>
        </div>
    );
}

export default UserProfile;
